---
import heroBg from '$assets/hero-overlay.jpg'; /* replace with your actual PNG */
---
<section id="hero" class="section-wrap !py-20 relative overflow-hidden edge-rip">
  <!-- Background image strictly behind + non-interactive -->
    <div class="absolute inset-0 z-0 pointer-events-none">
        <img src={heroBg.src} alt="" class="size-full object-cover opacity-40" />
    </div>

  <!-- Vertical wordmark (non-interactive so it never blocks clicks) -->
  <div class="absolute left-4 top-1/2 -translate-y-1/2 [writing-mode:vertical-rl] rotate-180
              text-neutral-500 tracking-[0.2em] text-sm md:text-base select-none pointer-events-none">
    GRAPHICSBYSTORM
  </div>

  <div class="relative z-10 grid gap-6 md:grid-cols-2 items-center">
    <h1 class="heading-hero">Design that sells.</h1>
    <p class="text-neutral-300">High-impact graphics, branding, and motion for teams that move fast.</p>
    <div class="flex gap-3">
      <a href="#work" class="btn-primary hover-lift">See work</a>
      <a href="#contact" class="btn-ghost hover-lift">Start a project</a>
    </div>
</div>
</section>
